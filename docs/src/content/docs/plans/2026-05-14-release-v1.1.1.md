---
title: "2026-05-14: Release v1.1.1"
description: "Patch release v1.1.1 — history filters, distinct colors, docker:dev always rebuilds, 15-month seed history, docs build fixes."
---

# Release v1.1.1

## Scope

Patch release (`v1.1.0` → `v1.1.1`). No breaking changes, no new user-facing features.
All changes are bug fixes and developer-experience improvements.

## Changes since v1.1.0

| Commit | Type | Summary |
|---|---|---|
| `185cf43` | fix | Dashboard history filters use asset snapshot dates (not portfolio snapshots); distinct colors for By Type / By Category |
| `2375915` | fix | `docker:dev` always generates version + layer-cached rebuild; seed adds 15-month asset snapshot history |
| `03fa9be` | fix | 4 plan doc titles with embedded `title:` key that broke the docs Docker build |
| `b1dace7` | fix | Tauri build: install full backend deps in `tauri-build.sh` |
| `6558f43` | fix | Tauri: share DB with `docker:prod`; fix version in prod builds |
| `e1fd317` | docs | Strengthen AGENTS.md convention #8 |
| `ec8686a` | docs | Fix plan doc filenames (add missing day) and YAML frontmatter |

## SemVer rationale

- No `feat` commits → no minor bump
- No breaking changes → no major bump
- Multiple `fix` commits → **patch bump**: `1.1.0` → **`1.1.1`**

## Release command

```bash
npm run release -- 1.1.1
```

This:
1. Validates semver format
2. Checks working tree is clean
3. Bumps `package.json` (×4), `Cargo.toml`, `tauri.conf.json`
4. Commits `chore: release v1.1.1`
5. Pushes HEAD to `origin`
6. Tags `v1.1.1`
7. Pushes tag to `origin`

## Execution Summary

**Commit**: `dfc4643` · **Tag**: `v1.1.1`

### Actual changes

- 6 version files bumped: `package.json` (×4), `src-tauri/Cargo.toml`, `src-tauri/tauri.conf.json`
- Commit `chore: release v1.1.1` pushed to `main`
- Tag `v1.1.1` pushed to `origin`

### Deviations from plan
None

### Test results
| Gate | Result |
|---|---|
| Backend unit | ✅ 315 tests (pre-release baseline) |
| Backend e2e  | ✅ 70 tests (pre-release baseline) |
| Frontend unit | ✅ 393 tests (pre-release baseline) |
| Frontend e2e  | ⏭ skipped |

### Key discoveries
None — release went cleanly on first attempt.
